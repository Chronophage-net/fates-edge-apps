name: Build and Package Apps

on:
  push:
    branches:
      - main
    paths:
      - 'ttrpg/utilities/**'
      - '.github/workflows/build-package-apps.yml'
  workflow_dispatch:
    inputs:
      release_version:
        description: 'Release version (e.g., v1.0.0)'
        type: string
        required: false
      create_release:
        description: 'Create GitHub Release'
        type: boolean
        default: false

env:
  NODE_VERSION: 24
  PYTHON_VERSION: 3.11

# Global defaults for all jobs
defaults:
  run:
    shell: bash

jobs:
  build-web-client:
    name: Build Web Client
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
        
      - name: Install and build web client
        working-directory: ttrpg/utilities/javascript/fates-edge-web-client
        run: |
          echo "Installing dependencies..."
          npm install
          
          echo "Building web client..."
          if grep -q '"build"' package.json 2>/dev/null; then
            npm run build
          else
            echo "No build script found, using static files"
            mkdir -p dist
            cp -r . dist/ 2>/dev/null || true
          fi
          
          echo "Build complete"

      - name: Package web client
        run: |
          mkdir -p artifacts
          tar -czf artifacts/fates-edge-web-client.tar.gz \
            -C ttrpg/utilities/javascript/fates-edge-web-client \
            --exclude='node_modules' \
            --exclude='.git' \
            --exclude='*.log' \
            .
          echo "Created fates-edge-web-client.tar.gz ($(du -h artifacts/fates-edge-web-client.tar.gz | cut -f1))"

      - name: Upload web client artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-web-client
          path: artifacts/fates-edge-web-client.tar.gz
          retention-days: 30
          if-no-files-found: error

  build-desktop-client:
    name: Build Desktop Client
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
         
      - name: Install desktop client
        working-directory: ttrpg/utilities/javascript/fates-edge-desktop-client
        run: |
          echo "Installing dependencies..."
          npm install
          echo "Install complete"

      - name: Package desktop client
        run: |
          mkdir -p artifacts
          tar -czf artifacts/fates-edge-desktop-client.tar.gz \
            -C ttrpg/utilities/javascript/fates-edge-desktop-client \
            --exclude='node_modules' \
            --exclude='.git' \
            --exclude='*.log' \
            .
          echo "Created fates-edge-desktop-client.tar.gz ($(du -h artifacts/fates-edge-desktop-client.tar.gz | cut -f1))"

      - name: Upload desktop client artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-desktop-client
          path: artifacts/fates-edge-desktop-client.tar.gz
          retention-days: 30
          if-no-files-found: error

  build-socket-server:
    name: Build Socket Server
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
         
      - name: Install socket server
        working-directory: ttrpg/utilities/javascript/fates-edge-socket-server
        run: |
          echo "Installing dependencies..."
          npm install
          echo "Install complete"

      - name: Package socket server
        run: |
          mkdir -p artifacts
          tar -czf artifacts/fates-edge-socket-server.tar.gz \
            -C ttrpg/utilities/javascript/fates-edge-socket-server \
            --exclude='node_modules' \
            --exclude='.git' \
            --exclude='*.log' \
            .
          echo "Created fates-edge-socket-server.tar.gz ($(du -h artifacts/fates-edge-socket-server.tar.gz | cut -f1))"

      - name: Upload socket server artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-socket-server
          path: artifacts/fates-edge-socket-server.tar.gz
          retention-days: 30
          if-no-files-found: error

  build-discord-bot:
    name: Build Discord Bot
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
  

      - name: Install Discord botcache
        working-directory: ttrpg/utilities/fates-edge-discord-bot
        run: |
          echo "Installing dependencies..."
          npm install
          echo "Install complete"

      - name: Package Discord bot
        run: |
          mkdir -p artifacts
          tar -czf artifacts/fates-edge-discord-bot.tar.gz \
            -C ttrpg/utilities/fates-edge-discord-bot \
            --exclude='node_modules' \
            --exclude='.git' \
            --exclude='*.log' \
            --exclude='.env' \
            .
          echo "Created fates-edge-discord-bot.tar.gz ($(du -h artifacts/fates-edge-discord-bot.tar.gz | cut -f1))"

      - name: Upload Discord bot artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-discord-bot
          path: artifacts/fates-edge-discord-bot.tar.gz
          retention-days: 30
          if-no-files-found: error

  package-roll20:
    name: Package Roll20 Integration
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Package Roll20 integration
        run: |
          mkdir -p artifacts
          tar -czf artifacts/fates-edge-roll20.tar.gz \
            -C ttrpg/utilities/fates-edge-roll20 \
            --exclude='.git' \
            --exclude='*.log' \
            .
          echo "Created fates-edge-roll20.tar.gz ($(du -h artifacts/fates-edge-roll20.tar.gz | cut -f1))"

      - name: Upload Roll20 artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-roll20
          path: artifacts/fates-edge-roll20.tar.gz
          retention-days: 30
          if-no-files-found: error

  package-foundry-bridge:
    name: Package Foundry VTT Bridge
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Package Foundry VTT bridge
        run: |
          mkdir -p artifacts
          tar -czf artifacts/foundry-fates-edge-bridge.tar.gz \
            -C ttrpg/utilities/foundry_fates-edge-bridge \
            --exclude='.git' \
            --exclude='*.log' \
            .
          echo "Created foundry-fates-edge-bridge.tar.gz ($(du -h artifacts/foundry-fates-edge-bridge.tar.gz | cut -f1))"

      - name: Upload Foundry bridge artifact
        uses: actions/upload-artifact@v4
        with:
          name: foundry-fates-edge-bridge
          path: artifacts/foundry-fates-edge-bridge.tar.gz
          retention-days: 30
          if-no-files-found: error

  build-python-client:
    name: Build Python Client
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install build tools
        run: |
          python -m pip install --upgrade pip
          pip install build wheel

      - name: Build Python client package
        working-directory: ttrpg/utilities/python/fates-edge-python-client
        run: |
          python -m build --sdist --wheel

      - name: Package Python client
        run: |
          mkdir -p artifacts
          # Copy sdist
          cp ttrpg/utilities/python/fates-edge-python-client/dist/*.tar.gz artifacts/fates-edge-python-client.tar.gz 2>/dev/null || true
          # Copy wheel
          cp ttrpg/utilities/python/fates-edge-python-client/dist/*.whl artifacts/ 2>/dev/null || true
          echo "Created Python client packages"

      - name: Upload Python client artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-python-client
          path: artifacts/
          retention-days: 30
          if-no-files-found: error

  package-python-tool:
    name: Package Python CLI Tool
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Package Python CLI tool
        run: |
          mkdir -p artifacts
          tar -czf artifacts/fates-edge-python-tool.tar.gz \
            -C ttrpg/utilities/python/fates_edge_tool \
            --exclude='.git' \
            --exclude='*.log' \
            --exclude='*.pyc' \
            --exclude='__pycache__' \
            .
          echo "Created fates-edge-python-tool.tar.gz ($(du -h artifacts/fates-edge-python-tool.tar.gz | cut -f1))"

      - name: Upload Python tool artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-python-tool
          path: artifacts/fates-edge-python-tool.tar.gz
          retention-days: 30
          if-no-files-found: error

  create-release:
    name: Create Release
    runs-on: ubuntu-latest
    needs:
      - build-web-client
      - build-desktop-client
      - build-socket-server
      - build-discord-bot
      - package-roll20
      - package-foundry-bridge
      - build-python-client
      - package-python-tool
    if: github.event.inputs.create_release == 'true' || github.ref == 'refs/heads/main'
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts/

      - name: List downloaded artifacts
        run: |
          echo "Downloaded artifacts:"
          find artifacts -type f -name "*.tar.gz" -o -name "*.whl" | sort
          echo ""
          echo "Artifact sizes:"
          find artifacts -type f -name "*.tar.gz" -o -name "*.whl" -exec du -h {} \; | sort

      - name: Create release assets directory
        run: |
          mkdir -p release-assets
          # Find and copy all tarballs and wheels
          find artifacts -type f \( -name "*.tar.gz" -o -name "*.whl" \) -exec cp {} release-assets/ \;
          echo "Release assets:"
          ls -la release-assets/

      - name: Generate checksums
        working-directory: release-assets
        run: |
          sha256sum * > checksums.txt
          echo "Checksums generated:"
          cat checksums.txt

      - name: Create combined release tarball
        run: |
          tar -czf fates-edge-apps-all.tar.gz -C release-assets .
          echo "Created fates-edge-apps-all.tar.gz ($(du -h fates-edge-apps-all.tar.gz | cut -f1))"

      - name: Upload combined release artifact
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-apps-all
          path: fates-edge-apps-all.tar.gz
          retention-days: 30
          if-no-files-found: error

      - name: Upload individual release artifacts
        uses: actions/upload-artifact@v4
        with:
          name: fates-edge-release-assets
          path: release-assets/
          retention-days: 30
          if-no-files-found: error

      - name: Create GitHub Release
        if: github.event.inputs.create_release == 'true' && github.event.inputs.release_version != ''
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.event.inputs.release_version }}
          name: Release ${{ github.event.inputs.release_version }}
          draft: false
          prerelease: false
          files: |
            release-assets/*.tar.gz
            release-assets/*.whl
            release-assets/checksums.txt
            fates-edge-apps-all.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
